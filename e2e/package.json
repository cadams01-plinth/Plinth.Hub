{
  "name": "plinth-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright acceptance tests — SPEC §12 (AT-01…AT-15).",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "codegen": "playwright codegen $PLINTH_E2E_BASE_URL"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.2",
    "@supabase/supabase-js": "^2.45.4",
    "jose": "^5.6.3"
  }
}
